// Configurations for extension. 

let config = {
   // startDate: '2019-06-15',
   // totalHrs: 400,
   // endDate: '2019-11-30',
   playbackRate: 1.5, // Default video playback rate  0.5 <= playbackRate <= 2
   quality: "360p", // Default video quality [ "360p", "540p","720p", "1080p" ]
   thingspeak: false,
   dropbox: false,
   jsonbin: false 
}

export default config;
